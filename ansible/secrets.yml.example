# Copy this file to secrets.yml and provide your own values.
# secrets.yml is ignored by Git and must not be committed.
db_name: "company_db"
db_username: "admin"
db_password: "REPLACE_WITH_A_SECURE_PASSWORD"
rds_endpoint: "REPLACE_WITH_RDS_ENDPOINT"
